CREATE TABLE bike
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    model VARCHAR(255),
    color VARCHAR(255),
    size  INTEGER,
    price DECIMAL,
    CONSTRAINT pk_bike PRIMARY KEY (id)
);